# List the start up tasks. Learn more https://www.gitpod.io/docs/config-start-tasks/
tasks:
  - name: Install PD CLI
    init: |
      curl https://cli.pipedream.com/linux/amd64/latest/pd.zip && \
      unzip pd.zip -d /usr/bin/pd
    command: pd

